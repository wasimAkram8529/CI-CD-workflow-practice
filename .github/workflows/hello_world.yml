name: "Hello world workflow"

on:
  push:
    branches:
      - master

jobs:
  hello_world:
    runs-on: windows-latest
    steps:
      - name: "hello world"
        run: echo "Hello world"

      - name: "second job"
        run: echo "Hii workflow"  
